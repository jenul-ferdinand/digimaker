{
  "name": "digimaker-monorepo",
  "version": "1.2.33",
  "private": true,
  "description": "Converts .docx lesson sheets into stylised PDFs",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/server",
    "packages/frontend",
    "packages/docling-cleaner"
  ],
  "scripts": {
    "build": "npm run build:core && npm run build:frontend && npm run build:cli && npm run build:server",
    "build:core": "npm run build -w @digimakers/core",
    "build:cli": "npm run build -w @digimakers/cli",
    "build:server": "npm run build -w @digimakers/server",
    "build:frontend": "npm run build -w @digimakers/frontend",
    "dev:frontend": "npm run start -w @digimakers/frontend",
    "dev:cli": "npm run dev -w @digimakers/cli",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,scss,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,css,scss,md}\"",
    "version:patch": "npm version patch --workspaces --include-workspace-root --no-git-tag-version",
    "version:minor": "npm version minor --workspaces --include-workspace-root --no-git-tag-version",
    "version:major": "npm version major --workspaces --include-workspace-root --no-git-tag-version",
    "release": "node scripts/release.js",
    "prepare": "simple-git-hooks"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "**/*.{ts,tsx,js,jsx,json,css,scss,md}": [
      "prettier --write"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jenul-ferdinand/digimaker.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jenul-ferdinand/digimaker/issues"
  },
  "homepage": "https://github.com/jenul-ferdinand/digimaker#readme",
  "devDependencies": {
    "chalk": "^5.6.2",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.0",
    "simple-git-hooks": "^2.13.1"
  }
}
